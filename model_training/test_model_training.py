import pandas as pd
import numpy as np


def test_dataloader():
    """
    This test is focused on all logic pretaining to the auto-regressive data loader:
    - Ensures subject grouping is working
    - Ensures that the output dimenssion is correct (Batch, Sequence Length, features)
    - Ensures the feature dimension is adherent to the input and output shapes
    - Ensures padding is handled properly
    - 
    """
    print("Dataloader test needs to be created")
